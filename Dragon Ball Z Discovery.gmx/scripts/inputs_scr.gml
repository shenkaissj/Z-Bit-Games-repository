// Checking for inputs
left_walk = keyboard_check(ord('A'));
right_walk = keyboard_check(ord('D'));
jump_press = keyboard_check_pressed(ord('W'));
punch_press = keyboard_check_pressed(ord('K'));
charge_press = keyboard_check(ord('C'));
